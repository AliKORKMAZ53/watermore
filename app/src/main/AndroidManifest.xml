<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.watermore">

    <uses-permission android:name="android.permission.INTERNET" />

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:resizeable="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tektek">
        <activity
            android:name="com.example.watermore.FullscreenSplashActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="Watermore"
            android:theme="@style/Theme.Tektek.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.watermore.GraphicActivity" />
        <activity android:name="com.example.watermore.ReminderScreen" />
        <activity android:name="com.example.watermore.HealthStats" />
        <activity
            android:name="com.example.watermore.usernamesPop"
            android:theme="@style/PopMe" />
        <activity
            android:name="com.example.watermore.AddWaterPopScreen"
            android:theme="@style/PopMe" />
        <activity android:name="com.example.watermore.MainPage"></activity>
        <activity android:name="com.example.watermore.LauncherActivity">

        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>